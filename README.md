# evmexam
* **Вопрос 1:** Косвенно-автоинкрементный метод адресации — Содержимое указанного в команде регистра интерпретируется процессором как адрес ячейки памяти, в которой находится адрес операнда, и после выборки операнда содержимое регистра (адрес адреса) увеличивается таким образом, чтобы указывать на адрес следующей по порядку ячейки.

* **Вопрос 2:** Регистровый метод адресации — В команде указывается номер регистра общего назначения, содержимое которого интерпретируется процессором как операнд.

* **Вопрос 3:** Непосредственный метод адресации — Операнд указывается непосредственно в команде, после кода операции. Процессор получает в этом случае адрес операнда непосредственно из своего регистра-счетчика команд.

* **Вопрос 4:** Абсолютный метод адресации это — Косвенно-автоинкрементный метод адресации через регистр счетчик команд.

* **Вопрос 5:** Косвенная адресация со смещением (индексный метод адресации) — В дополнительном слове команды указывается так называемое смещение (индексное слово). Исполнительный адрес операнда определяется как сумма содержимого указанного в команде регистра и смещения (индексного слова).

* **Вопрос 6:** Косвенно-относительный метод адресации это — Косвенно-индексный метод адресации через регистр счетчик команд.

* **Вопрос 7:** Относительный метод адресации это — Индексный метод адресации через регистр счетчик команд.

* **Вопрос 8:** Автодекрементный способ адресации — При интерпретации команды содержимое указанного в команде регистра вначале уменьшается на 1 или 2, после чего уменьшенное содержимое регистра интерпретируется процессором как адрес ячейки памяти, в которой находится операнд.

* **Вопрос 9:** Регистровый метод адресации — Содержимое указанного в команде регистра интерпретируется процессором как операнд.

* **Вопрос 10:** Автодекременный метод адресации — При интерпретации команды содержимое указанного в команде регистра вначале уменьшается на 1 или 2, после чего уменьшенное содержимое регистра интерпретируется процессором как адрес ячейки памяти, в которой находится операнд.

* **Вопрос 11:** Автоинкрементный метод адресации — Содержимое указанного в команде регистра интерпретируется процессором как адрес ячейки памяти, в которой находится операнд, и после выборки операнда содержимое регистра увеличивается на 1 или 2 таким образом, чтобы указывать на адрес следующей по порядку ячейки памяти.

* **Вопрос 12:** Непосредственный метод адресации это — Автоинкрементный метод адресации через регистр счетчик команд.

* **Вопрос 13:** Косвенно-регистровый метод адресации — Содержимое указанного в команде регистра интерпретируется процессором как адрес операнда.

* **Вопрос 14:** Косвенно-автодекременный метод адресации — При интерпретации команды содержимое указанного в команде регистра вначале уменьшается на 2, после чего уменьшенное содержимое регистра интерпретируется процессором как адрес ячейки памяти, в которой находится адрес операнда.

* **Вопрос 15:** Относительный метод адресации — Во втором слове команды указывается относительный адрес операнда, т.е. величина смещения адреса операнда относительно адреса самой команды (текущего содержимого регистра-счетчика команд процессора).

* **Вопрос 16:** Автоинкрементный метод адресации — Содержимое указанного в команде регистра интерпретируется процессором как адрес ячейки памяти, в которой находится операнд, и после выборки операнда содержимое регистра увеличивается на 1 или 2 таким образом, чтобы указывать на адрес следующей по порядку ячейки памяти.

* **Вопрос 17:** Абсолютный метод адресации — Во втором слове команды, т.е. в следующей за кодом команды ячейке памяти, указывается адрес операнда.

* **Вопрос 18:** Программа называется перемещаемой, если — при ее размещении в разных местах памяти не требуется вносить изменений в ее код.

* **Вопрос 19:** Какой метод адресации следует использовать в перемещаемой программе для адресации данных, расположенных в теле программы? — относительный метод адресации.

* **Вопрос 20:** Какой метод адресации следует использовать в перемещаемой программе для адресации регистров внешних устройств? — абсолютный метод адресации.

* **Вопрос 21:** Память, организованная в виде стека — Записанные данные могут быть последовательно прочитаны только в порядке, обратном порядку их записи.

* **Вопрос 22:** Память с произвольным доступом — В каждый момент времени для чтения доступна любая ячейка памяти независимо от ее расположения.

* **Вопрос 23:** Для запоминающего устройства, организованного в виде стека, имеет место следующее — В каждый момент времени для чтения доступна только ячейка памяти, являющаяся вершиной стека.

* **Вопрос 24:** Способ доступа к ячейкам памяти организованным в виде стека — Данные, записанные последними, читаются первыми.
* 
* **Вопрос 25:** Для записи числа в стек используется — автодекрементный метод адресации.
* 
* **Вопрос 26:** Память с произвольным доступом — В каждый момент времени для чтения доступна любая ячейка памяти независимо от ее расположения.

* **Вопрос 27:** Для чтения числа из стека используется — автоинкрементный метод адресации.

* **Вопрос 28:** Память с произвольным доступом — Для доступа к данным надо указать адрес ячейки памяти / Информация об один раз прочитанных данных не теряется.

* **Вопрос 29:** Регистр - указатель стека всегда содержит — адрес вершины стека.

* **Вопрос 30:** Способ доступа к ячейкам памяти с произвольным доступом — В каждый момент времени можно прочитать данные из любой ячейки.

* **Вопрос 31:** Память, организованная в виде стека — Информация об один раз прочитанных данных теряется.

* **Вопрос 32:** Команда условного перехода используется для — изменения содержимого регистра-счетчика команд в случае выполнения заданного условия.

* **Вопрос 33:** Адрес возврата из подпрограммы запоминается — в стеке.

* **Вопрос 34:** Для перехода к подпрограмме — нельзя использовать команду безусловного перехода.

* **Вопрос 35:** Для возврата из подпрограммы — нельзя использовать команду условного перехода.

* **Вопрос 36:** Подпрограммой называется - — программный модуль, к которому можно обращаться из любого места программы любое число раз.

* **Вопрос 37:** При выполнении команды возврат из подпрограммы — адрес возврата выталкивается из вершины стека и помещается в регистр-счетчик команд.

* **Вопрос 38:** При выполнении команды вызов подпрограммы — содержимое регистра-счетчика команд процессора пересылается в стек.

* **Вопрос 39:** Вложенные подпрограммы это подпрограммы, — вызываемые из других подпрограмм.

* **Вопрос 40:** Для компьютера с RISC-архитектурой является характерным — Небольшое число простых форматов команд.

* **Вопрос 41:** Для компьютера с CISC-архитектурой является характерным — Небольшое число регистров общего назначения.

* **Вопрос 42:** Для компьютера с RISC-архитектурой является характерным — Небольшое число простых способов адресации.

* **Вопрос 43:** RISC-процессор это процессор с — сокращенным набором команд.

* **Вопрос 44:** Для компьютера с RISC-архитектурой является характерным — Сокращенный набор команд.

* **Вопрос 45:** Для компьютера с CISC-архитектурой является характерным — Большое число сложных форматов команд.

* **Вопрос 46:** Для компьютера с RISC-архитектурой является характерным — Увеличенное число регистров общего назначения.

* **Вопрос 47:** Для компьютера с CISC-архитектурой является характерным — Расширенное число разнообразных способов адресации.

* **Вопрос 48:** CISC-процессор это процессор с — процессор с фон-неймановской архитектурой.


* **Вопрос 49:** Для компьютера с CISC-архитектурой является характерным — Расширенный набор команд.


* **Вопрос 50:** Что такое регистр состояния внешнего устройства? — Регистр, находящийся в интерфейсе внешнего устройства, с помощью которого осуществляется проверка готовности внешнего устройства к приему или передаче данных.


* **Вопрос 51:** Каким образом в программным путем можно определить готовность внешнего устройства к обмену информацией? — Проверяется состояние соответствующего разряда в регистре состояния внешнего устройства.


* **Вопрос 52:** Для программно-управляемого ввода/вывода по опросу флага готовности характерны: — Простота программирования / Простота аппаратной реализации.


* **Вопрос 53:** Для того, чтобы осуществить передачу одного байта данных внешнему устройству в режиме ввода-вывода по опросу флага готовности внешнего устройства, необходимо: — Переслать байт в регистр данных внешнего устройства.


* **Вопрос 54:** Состоянием разряда (флага) готовности внешнего устройства в его регистре состояния управляет: — внешнее устройство.


* **Вопрос 55:** Прием или передача данных осуществляется через — регистр данных внешнего устройства.


* **Вопрос 56:** Режим ввода-вывода данных по опросу готовности внешнего устройства осуществляется: — под управлением выполняемой процессором программы.


* **Вопрос 57:** Что такое регистр данных внешнего устройства? — Регистр, находящийся в интерфейсе внешнего устройства, через который осуществляется прием или передача данных.


* **Вопрос 58:** При передаче массива данных во внешнее устройство в режиме ввода-вывода по опросу флага готовности внешнего устройства контроль количества переданных байтов или слов осуществляет — Выполняемая процессором программа.


* **Вопрос 59:** Готовность или неготовность внешнего устройства к вводу-выводу проверяется в — регистре состояния внешнего устройства.


* **Вопрос 60:** Адрес возврата из прерывания запоминается — В стеке.


* **Вопрос 61:** При включении питания компьютера разряд разрешения/запрета прерывания для внешнего устройства в регистре состояния его интерфейса автоматически устанавливается в состояние: — прерывание запрещено.


* **Вопрос 62:** При получении от внешнего устройства сигнала требования прерывания процессор — Продолжает выполнение текущей команды до ее завершения.


* **Вопрос 63:** При передаче массива данных во внешнее устройство в режиме прерывания контроль количества переданных байтов осуществляет — Выполняемая процессором программа.


* **Вопрос 64:** Ввод-вывод данных в режиме прерывания осуществляется: — по инициативе внешнего устройства / под управлением выполняемой процессором программы.


* **Вопрос 65:** Что делает внешнее устройство при получении от процессора сигнала предоставления прерывания? — Передает процессору адрес своего вектора прерывания.


* **Вопрос 66:** Можно ли для возврата из подпрограммы использовать команду возврат из прерывания? — Нельзя.


* **Вопрос 67:** Что необходимо сделать для того, чтобы запретить прерывание ВСЕМ внешним устройствам? — Установить состояние высокого приоритета процессора в его регистре состояния.


* **Вопрос 68:** После выполнения какого действия процессор посылает внешнему устройству сигнал предоставления прерывания? — После запоминания в стеке текущего содержимого регистра-счетчика команд и регистра состояния процессора.


* **Вопрос 69:** Можно ли для возврата из прерывания использовать команду возврат из подпрограммы? — Нельзя.


* **Вопрос 70:** Откуда появляется информация в векторе прерывания? — Устанавливается программным путем.


* **Вопрос 71:** Можно ли для возврата из прерывания использовать команду возврат из подпрограммы? — Нельзя, потому, что эта команда не обеспечивает полного восстановления прежнего (т.е. до прерывания) состояния процессора.


* **Вопрос 72:** При переходе в режим прерывания процессор запоминает в стеке: — Текущее содержимое регистра состояния процессора / Текущее содержимое регистра-счетчика команд.


* **Вопрос 73:** Каким образом можно запретить прерывание конкретному внешнему устройству? — Сбросить в нуль разряд приоритета в регистре состояния внешнего устройства.


* **Вопрос 74:** Какие принципиальные проблемы должны решаться при реализации ввода/вывода в режиме прерывания программы? — Работа основной (прерываемой) программы должна приостанавливаться без какого-либо ущерба для ее выполнения / Необходим механизм, позволяющий программным путем запрещать или разрешать прерывания всем или некоторым внешним устройствам, когда это необходимо.


* **Вопрос 75:** По сравнению с вводом выводом по опросу флага готовности для режима прерывания характерны: — Более сложная аппаратная реализация / Более высокая эффективность использования процессора / Более сложное программирование.


* **Вопрос 76:** Что такое вектор прерывания? — Ячейки памяти, в одной из которых указан адрес программы обслуживания прерывания от конкретного устройства.


* **Вопрос 77:** Каким образом процессор узнает адрес, по которому в памяти ЭВМ находится программа обслуживания внешнего устройства, затребовавшего прерывание? — Считывает этот адрес из первой ячейки вектора прерывания внешнего устройства.


* **Вопрос 78:** Какие операции необходимо осуществить в программе, для того, чтобы обеспечить возможность работы с внешним устройством в режиме прерывания? — Установить в первой ячейке вектора прерывания адрес программы обслуживания прерывания.


* **Вопрос 79:** Что происходит при выполнении команды возврат из прерывания? — Выталкивается из стека и записывается в регистр состояния процессора прежнее слово состояния процессора / Адрес возврата в прерванную программу из вершины стека пересылается в регистр-счетчик команд процессора.


* **Вопрос 80:** Для чего при реализации механизма прерывания необходим стек? — Для запоминания текущего содержимого регистра-счетчика команд.


* **Вопрос 81:** Состояние разряда разрешения/запрета прерывания внешнего устройства в его регистре состояния устанавливается: — из программы соответствующей командой.


* **Вопрос 82:** Что произойдет, если два внешних устройства одновременно затребуют прерывание? — Прерывание будет предоставлено устройству с более высоким приоритетом.


* **Вопрос 83:** Что такое "вложенное" прерывание? — Прерывание, которое происходит во время обслуживания другого прерывания.


* **Вопрос 84:** Каким образом при окончании обслуживания внешнего устройства, вызвавшего прерывание, осуществляется возврат в прерванную программу? — Выполняется команда возврат из прерывания.


* **Вопрос 85:** Для перехода к подпрограмме — нельзя использовать команду условного перехода.


* **Вопрос 86:** Для возврата из подпрограммы — нельзя использовать команду безусловного перехода.


* **Вопрос 87:** Целесообразно ли использовать для обмена данными с жестким диском режим прямого доступа к памяти? — Да целесообразно.


* **Вопрос 88:** При приеме массива данных из внешнего устройства в режиме прямого доступа к памяти контроль количества переданных байтов или слов осуществляет — Контроллер внешнего устройства.


* **Вопрос 89:** Для перехода к режиму предоставления внешнему устройству прямого доступа к памяти — Процессор не должен доводить выполнение текущей команды до конца.


* **Вопрос 90:** По сравнению с другими режимами для ввода-вывода в режиме прямого доступа к памяти характерно: — Более сложная аппаратная реализация / Более высокая скорость передачи данных / Более высокая эффективность использования процессора.


* **Вопрос 91:** В режиме прямого доступа к памяти управление вводом-выводом осуществляется: — Внешним устройством.


* **Вопрос 92:** При передаче массива данных во внешнее устройство в режиме прямого доступа к памяти контроль количества переданных байтов или слов осуществляет — Контроллер внешнего устройства.


* **Вопрос 93:** Целесообразно ли использовать для ввода данных с клавиатуры режим прямого доступа к памяти? — Не целесообразно.


* **Вопрос 94:** Для каких из перечисленных внешних устройств целесообразно использовать ввод/вывод информации в режиме прямого доступа к памяти? — Внешнее запоминающее устройство.


* **Вопрос 95:** Что должен "уметь" делать контроллер прямого доступа к памяти? — Посчитывать число переданных байтов / Формировать сигнал требования прямого доступа.


* **Вопрос 96:** Что означает аббревиатура DMA? — Прямой доступ к памяти.


* **Вопрос 97:** После выполнения каких действий процессор посылает внешнему устройству сигнал предоставления прямого доступа к памяти? — После завершения процессором очередного цикла обращения к каналу (к оперативной памяти).


* **Вопрос 98:** При передаче массива данных во внешнее устройство в режиме прямого доступа к памяти формирование адресов ячеек памяти, из которых осуществляется пересылка данных, осуществляет — Контроллер внешнего устройства.


* **Вопрос 99:** Что делает внешнее устройство после получения от процессора сигнала предоставления прямого доступа к памяти? — Осуществляет управление магистралями компьютера и его памятью для передачи данных.


* **Вопрос 100:** Какой режим ввода вывода целесообразно использовать для обмена данными с клавиатурой? — Режим прерывания.


* **Вопрос 101:** Какой режим ввода вывода целесообразно использовать для обмена данными с видеомонитором? — Режим прямого доступа к памяти.


* **Вопрос 102:** Расположите перечисленные режимы ввода/вывода в порядке возрастания степени их пригодности для передачи больших массивов данных — Прямой доступ к памяти: Хорошо пригоден, Режим прерывания: Плохо пригоден, По опросу готовности: Практически не пригоден.


* **Вопрос 103:** Какой режим ввода-вывода целесообразно использовать для обмена данными с жестким диском? — Режим прямого доступа к памяти.


* **Вопрос 104:** Расположите перечисленные режимы ввода/вывода в порядке увеличения эффективности использования процессорного времени — Прямой доступ к памяти: Самая высокая эффективность, Режим прерывания: Средняя эффективность, По опросу готовности: Самая низкая эффективность.


* **Вопрос 105:** Расположите перечисленные режимы ввода/вывода в порядке увеличения сложности их реализации — По опросу готовности: Минимальная сложность, Режим прерывания: Средняя сложность, Прямой доступ к памяти: Максимальная сложность.


* **Вопрос 106:** Расположите перечисленные режимы ввода/вывода в порядке возрастания скорости передачи данных — Прямой доступ к памяти: Максимальная скорость, Режим прерывания: Средняя скорость, По опросу готовности: Минимальная скорость.


* **Вопрос 107:** Какой режим ввода вывода целесообразно использовать для обмена данными со сканером? — Режим прямого доступа к памяти.


* **Вопрос 108:** Расположите перечисленные режимы ввода/вывода в порядке возрастания степени их пригодности для передачи данных для отображения на экране монитора — Прямой доступ к памяти: Хорошо пригоден, Режим прерывания: Плохо пригоден, По опросу готовности: Практически не пригоден.


* **Вопрос 109:** Расположите перечисленные режимы ввода/вывода в порядке возрастания степени их пригодности для обмена данными со сканером — Прямой доступ к памяти: Хорошо пригоден, Режим прерывания: Плохо пригоден, По опросу готовности: Практически не пригоден.


* **Вопрос 110:** Расположите перечисленные режимы ввода/вывода в порядке возрастания степени их пригодности для обмена данными с внешними запоминающими устройствами — Прямой доступ к памяти: Хорошо пригоден, Режим прерывания: Плохо пригоден, По опросу готовности: Практически не пригоден.


* **Вопрос 111:** Сколько разрядов в физическом адресе требуется для адресации памяти объемом 1 Мегабайт? — 20.


* **Вопрос 112:** Кэш-память — используется для ускорения работы компьютера.


* **Вопрос 113:** Что такое свопинг — Процедура загрузки виртуальных страниц задачи с диска в физическую память взамен выгружаемых неиспользуемых страниц.


* **Вопрос 114:** Кэш-память — размещается между процессором и основной оперативной памятью.


* **Вопрос 115:** Сколько разрядов в физическом адресе требуется для адресации памяти объемом 2 Мегабайт? — 21.


* **Вопрос 118:** К какому объему памяти можно адресоваться с помощью 30-разрядного адреса? — 1 Гигабайт.


* **Вопрос 119:** Могут ли 16-разрядные процессоры работать с памятью объемом 2 Мегабайта? — Могут некоторые 16-разрядные процессоры.


* **Вопрос 120:** К какому объему памяти можно адресоваться с помощью 20-разрядного адреса? — 1 Мегабайт.


* **Вопрос 121:** Физической памятью называют — множество имеющихся в ОЗУ компьютера ячеек оперативной памяти.


* **Вопрос 122:** Сколько разрядов в физическом адресе требуется для адресации памяти объемом 1 Гигабайт? — 30.


* **Вопрос 124:** Преобразование виртуального адреса в физический адрес осуществляется: — прибавления двоичной константы к виртуальному адресу.


* **Вопрос 125:** Виртуальные станицы одной задачи должны загружаться в физическую память — распределяясь по любым свободным областях памяти.


* **Вопрос 126:** Регистры адреса страниц в диспетчере памяти процессора используются для: — хранения констант перемещения для виртуальных страниц.


* **Вопрос 128:** Кэш-память это — промежуточная буферная память, предназначенная для согласования быстродействия процессора и основной памяти.


* **Вопрос 129:** Сколько Разрядов в физическом адресе требуется для адресации памяти объемом 4 Гигабайта? — 32.


* **Вопрос 130:** К какому объему памяти можно адресоваться с помощью 32-разрядного адреса? — 4 Гигабайта.


* **Вопрос 131:** К какому объему памяти можно адресоваться с помощью 16-разрядного адреса? — 64 Килобайта / 65 536 Байт.


* **Вопрос 132:** Сколько разрядов в физическом адресе требуется для адресации памяти объемом 4 Мегабайт? — 22.


* **Вопрос 133:** Может ли виртуальная память (логическое адресное пространство) быть больше физической памяти? — Да может.


* **Вопрос 134:** К какому объему памяти можно адресоваться с помощью 22-разрядного адреса — 4 Мегабайта.


* **Вопрос 135:** Виртуальной памятью (виртуальным адресным пространством) называют — адресное пространство, которое представляется разработчику при создании кода программы.


* **Вопрос 136:** Для многопроцессорных кластерных систем является характерным: — Возможность наращивания мощности комплекса в процессе эксплуатации / Использование группы полноценных компьютеров взаимосвязанных через локальную сеть / Отказ одного узла не приводит к потере работоспособности всего комплекса / Каждый процессор работает со своим ОЗУ.


* **Вопрос 137:** Для симметричных многопроцессорных систем является характерным: — Наличие двух или более одинаковых или близких по характеристикам процессоров, установленных на одной материнской плате / Доступ процессоров к общим средствам ввода-вывода / Наличие общей памяти, к которой имеют доступ все процессоры / Отказ одного процессора не приводит потере работоспособности комплекса в целом.


* **Вопрос 138:** Распараллеливание алгоритма выполнения задачи это — одновременное выполнение набором операционных устройств операций, составляющих задачу, над множеством одновременно поступающих входных данных.


* **Вопрос 139:** Рисунок иллюстрирует вычислительную архитектуру типа - поток команд, одинарный поток данных — одинарный.


* **Вопрос 140:** Рисунок иллюстрирует: — последовательность микроопераций, составляющих машинную команду.


* **Вопрос 141:** Рисунок иллюстрирует вычислительную архитектуру типа поток команд, поток данных, мультипроцессор с распределенной памятью — множественный.


* **Вопрос 144:** Рисунок иллюстрирует принцип повышения производительности путем — распараллеливания выполнения операций.


* **Вопрос 145:** Для многопроцессорных кластерных систем является характерным: — Снижение соотношения цена/производительность / Могут использоваться компьютеры массового производства / Простая масштабируемость. Возможность включения в комплекс любого числа компьютеров / Возможность наращивания мощности комплекса в процессе эксплуатации.


* **Вопрос 146:** Для симметричных многопроцессорных систем является характерным — Управление всем комплексом общей операционной системой / Наличие общей памяти, к которой имеют доступ все процессоры / В случае отказа одного из процессоров задача может выполняться на другом.


* **Вопрос 153:** Команда условного перехода используется для — изменения содержимого регистра-счетчика команд в случае выполнения заданного условия.


* **Вопрос 154:** Особенности RISC-архитектуры — Небольшое число простых форматов команд / Сокращенный набор команд / Увеличенное число регистров общего назначения.


* **Вопрос 155:** Какой из перечисленных режимов ввода/вывода наиболее пригоден для работы с манипулятором мышь? — Режим прерывания.


* **Вопрос 158:** Для симметричных многопроцессорных систем является характерным — Наличие двух или более одинаковых или близких по характеристикам процессоров, установленных на одной материнской плате / Доступ процессоров к общим средствам ввода-вывода / Отказ одного процессора не приводит потере работоспособности комплекса в целом / Все процессоры способны выполнять одинаковый набор функций / Наличие общей памяти, к которой имеют доступ все процессоры.


* **Вопрос 159:** При передаче массива данных из внешнего устройства в оперативную память в режиме прямого доступа к памяти формирование адресов ячеек памяти осуществляет — Контроллер внешнего устройства.


* **Вопрос 160:** Для многопроцессорных кластерных систем является характерным — Каждый процессор работает со своим ОЗУ / Отказ одного процессора не приводит потере работоспособности всего комплекса / Использование группы полноценных компьютеров взаимосвязанных через локальную сеть / Отказ одного узла не приводит потере работоспособности всего комплекса.


* **Вопрос 161:** Каким образом можно запретить прерывание конкретному внешнему устройству? — Установить в ноль соответствующий разряд в регистре состояния внешнего устройства.


* **Вопрос 162:** Можно ли для возврата из прерывания использовать команду возврат из подпрограммы? — Нельзя, потому, что эта команда не пересылает в регистр состояния из стека слово состояния процессора.


* **Вопрос 163:** Для многопроцессорных кластерных систем является характерным — Простая масштабируемость. Возможность включения в комплекс любого числа компьютеров / Каждый процессор работает со своим ОЗУ / Могут использоваться компьютеры массового производства / Снижение соотношения цена/производительность / Возможность наращивания мощности комплекса в процессе эксплуатации.


* **Вопрос 164:** При размещении команд и данных в общей памяти ... — Эффективнее используется имеющийся объем оперативной памяти.


* **Вопрос 165:** Что такое адрес ячейки памяти ЭВМ? — Ее порядковый номер.


* **Вопрос 166:** Какая система счисления выбрана в фон-неймановской ЭВМ для внутреннего представления чисел? — Двоичная.


* **Вопрос 167:** Особенности организации памяти фон-неймановской ЭВМ и выборки из нее информации — Можно обращаться в любой момент к любой одной ячейке памяти по ее адресу / Память представляет собой набор одинаковых запоминающих ячеек.


* **Вопрос 168:** Для того, чтобы процессор мог выполнять программу, она должна размещаться ...? — В оперативной памяти компьютера.


* **Вопрос 169:** В фон-неймановской ЭВМ данные и команды размещаются ...? — И данные и команды размещаются в общей оперативной памяти.


* **Вопрос 170:** Память фон-неймановского компьютера называется памятью с _____ выборкой — произвольной.


* **Вопрос 171:** ЭВМ - это машина с _____ программой — хранимой.


* **Вопрос 172:** Анализ структуры двоичного кода содержимого ячейки памяти фон-неймановской ЭВМ: — не позволяет определить - команда это или данные.


* **Вопрос 173:** Наличие каких компонентов компьютера следует из формулы <ЭВМ - это машина с хранимой программой>? — Оперативная память / Процессор.


* **Вопрос 174:** Данные в ячейках памяти фон-неймановской ЭВМ представляются и хранятся: — В виде двоичного кода.


* **Вопрос 176:** Для того, чтобы при выполнении команд правильно отличать одни типы данных от других, например коды символов от чисел, в фон-неймановском компьютере — все приведенные ответы не верны.


* **Вопрос 178:** По содержимому ячейки памяти фон-неймановской ЭВМ: — нельзя определить команда это или данные.
	

* **Вопрос 190:** Процессор фон-неймановской ЭВМ получает адрес следующей команды — из регистра - счетчика команд.


* **Вопрос 191:** Содержимое, какого (каких) регистра (регистров) всегда интерпретируется процессором как адрес ячейки памяти, в которой размещена следующая команда выполняемой программы? — Регистра-счетчика команд.


* **Вопрос 192:** Укажите, какой (какие) из перечисленных блоков (элементов) фон-неймановской ЭВМ выполняет приведенную функцию - <Используется процессором как временная быстродействующая память для входных и выходных данных (операндов) АЛУ, а также реализации методов адресации операндов> — Регистр общего назначения.


* **Вопрос 193:** В каком регистре (регистрах) процессора можно посмотреть, произошел или нет при выполнении текущей команды перенос из старшего разряда? — Регистр состояния процессора.


* **Вопрос 194:** Укажите, какому (каким) из перечисленных понятий фон-неймановской ЭВМ соответствует приведенное определение - <Совокупность команд, которые способен выполнять процессор> — Система команд процессора.


* **Вопрос 195:** Если в текущей команде не указано, из какой ячейки памяти выбирать следующую команду, то в фон-неймановской ЭВМ: — процессор выбирает эту команду из ячейки памяти, следующей за ячейкой памяти текущей команды.


* **Вопрос 196:** Почему в фон-неймановской ЭВМ для внутреннего представления чисел выбрана двоичная система счисления? — Из-за более простой реализации устройств для операций над числами / Из-за более простой реализации устройств для хранения таких чисел.


* **Вопрос 197:** Для того, чтобы правильно находить команды выполняемой программы в оперативной памяти, не путая их с данными, в фон-неймановском компьютере — в состав процессора вводится регистр-счетчик команд.


* **Вопрос 201:** Рисунок иллюстрирует — последовательность микроопераций, составляющих машинную команду.


* **Вопрос 202:** Можно ли для возврата из прерывания использовать команду возврат из подпрограммы? — Нельзя, потому, что эта команда не восстанавливает прежнего (до прерывания) содержимого регистра состояния процессора.


* **Вопрос 203:** Программа называется перемещаемой, если — при ее размещении в разных местах памяти не требуется вносить изменений в ее код.


* **Вопрос 205:** В каком регистре (регистрах) процессора можно посмотреть, положительное или отрицательное число получилось в результате выполнения текущей команды? — Регистр состояния процессора.


* **Вопрос 206:** В процессоре фон-неймановского компьютера адрес следующей команды находится — в регистре-счетчике команд.


* **Вопрос 208:** Укажите, какой (каким) из перечисленных характеристик фон-неймановской ЭВМ соответствует приведенное определение - <Количество разрядов в операндах, над которыми процессор может совершать операции> — Разрядность процессора.


* **Вопрос 215:** Ввод-вывод данных в режиме прямого доступа к памяти осуществляется: — под управлением контроллера внешнего устройства / по инициативе внешнего устройства.


* **Вопрос 216:** В процессоре фон-неймановской ЭВМ для формирования адреса следующей команды служит — регистр-счетчик команд.


* **Вопрос 218:** Команды в ячейках памяти фон-неймановской ЭВМ представляются и хранятся: — В виде двоичного кода.


* **Вопрос 220:** Укажите, какой (какие) из перечисленных блоков (элементов) фон-неймановской ЭВМ выполняет приведенную функцию - <Выполняет арифметические и логические операций над двоичными числами или кодами> — Арифметико-логическое устройство.


* **Вопрос 221:** Содержимое, какого (каких) регистра (регистров) при считывании процессором из ячейки памяти очередной команды автоматически увеличивается и становится равным адресу следующей по порядку команды? — Регистра-счетчика команд.


* **Вопрос 222:** Укажите, какой (какие) из перечисленных блоков (элементов) фон-неймановской ЭВМ выполняет приведенную функцию - <Временное хранение кода текущей команды, выполняемой ЭВМ программы, на время ее выполнения> — Регистр общего назначения / Регистр команд.


* **Вопрос 223:** Укажите, какой (какие) из перечисленных блоков (элементов) фон-неймановской ЭВМ выполняет приведенную функцию - <Осуществляет управление всеми элементами центрального процессора> — Устройство управления.


* **Вопрос 226:** В каком регистре (регистрах) процессора можно посмотреть, произошло или нет при выполнении текущей команды арифметическое переполнение? — Регистр состояния процессора.


* **Вопрос 227:** Укажите, какому (каким) из перечисленных блоков (элементов) фон-неймановской ЭВМ соответствует приведенное определение - <Находится в оперативном запоминающем устройстве и предназначен(о) для указания адреса ячейки памяти, к которой происходит обращение> — Регистр адреса памяти.



*(Примечание: Ответы на вопросы, касающиеся конкретных рисунков, таблиц истинности и специфических диаграмм состояний (например, "В момент времени t1 схема..." или "Укажите правильные названия..."), были частично опущены или сокращены, так как они опираются на визуальный материал, отсутствующий в тексте, однако численные ответы к ним [например, 0 или 1] учтены там, где это имеет прямой смысловой контекст).*</Находится></Осуществляет></Временное></Выполняет></Количество></Совокупность></Используется></ЭВМ>
